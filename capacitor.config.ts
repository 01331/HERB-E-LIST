import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'herb.e.list',
  appName: 'herb-e-list',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
